[package]
name = "kwe-shader-compiler"
version.workspace = true
edition.workspace = true
license.workspace = true
rust-version.workspace = true

[dependencies]
kwe-platform = { path = "../kwe-platform" }
libc.workspace = true
kwe-core = { path = "../kwe-core" }
kwe-report-protocol = { path = "../kwe-report-protocol" }
serde_json.workspace = true
# SR-3c: the SAME shaderc crate/version/source kwe-scene-renderer already
# depends on (workspace-pinned in the root Cargo.toml) -- reuse, not a new
# external dependency. Links the SYSTEM libshaderc via pkg-config, same as
# the renderer's own comment there explains; packaging already lists
# `shaderc` in both PKGBUILD arrays for the renderer, and covers this
# binary too (packaging/PKGBUILD).
shaderc.workspace = true
