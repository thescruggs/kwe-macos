# Alpha M5c — manager playlist controller

M5c exposes the first manager-side playlist controller. Playlist names are
persisted in user settings, bounded to 256 entries, and exposed to QML for the
editor work that follows. The current gallery only shows existing playlists;
wallpaper ordering and renderer assignment remain the next slice.
