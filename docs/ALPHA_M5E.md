# Alpha M5e — playlist contents

M5e makes playlist membership persistent: selected wallpaper IDs are added,
deduplicated, capped at 1,024 entries, and removable through the controller
API. The manager shows the selected playlist's item count; detailed reorder and
transition controls remain next.
