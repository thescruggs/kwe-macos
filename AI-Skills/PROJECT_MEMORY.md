# AI-Skills — Project Memory

**Current as of:** 2026-08-22 (M3g merged; apply lane repaired; Alpha 1 package release 3 built and verified). Read `INSTRUCTIONS.md` first; `BETA_PLAN.md` is the living plan.

## Current repo state

- **Repo:** `/home/qcv123/gitProjects/KDE-Wallpaper-Engine` — KDE Plasma 6 Wallpaper Engine-compatible experience for Arch/CachyOS. **Beta work happens in per-slice worktrees** (`/home/qcv123/gitProjects/kwe-*`, deleted after merge); the trunk branch is `fix/qt611-gallery-delegates` (no upstream).
- **History (all ff-merged into the trunk):** `1833331` Qt 6.11 fix → `db8563d` AI-Skills → BETA_M1 (M1a–M1e) → BETA_M2 (M2a–M2e, `dc125d8`) → M3a–M3f (`d4ea8c2`+`5abc800`) → M4a `cffb7cd`+`f3376c7` → M4b `eb83966`+`e6152bd` → M4c `e5d69e0`+`520b2d7` → M4d `0d8a883` → M3g `3b368d2`+`0087c06` → Alpha package release 2 `ca3807e` → apply-lane fix `a747064` + release 3 `bd60301`. Full per-slice detail lives in BETA_PLAN.md's change log.
- **Installed:** `kde-wallpaper-engine-0.1.0.alpha.1-2` (installed 2026-08-22) — its manager cannot apply anything: the QML client types are unregistered and `wallpaper.outputs` is never requested (`docs/bugs/APPLY_NO_OUTPUTS.md`). **Built, install pending the maintainer's sudo:** `packaging/kde-wallpaper-engine-0.1.0.alpha.1-3-x86_64.pkg.tar.zst` (sha256 `ac30ff4fa85510d1980c8f37c97699f55e30528eb4da5195c9df26bdbae1f1aa`), built from `bd60301` and verified live from the archive.
- **Graphify:** code graph refreshed 2026-08-22 (3,826 nodes / 9,682 edges / 149 communities). Query with `graphify query "<question>"`. `metadata.json` still produces zero AST nodes; community labels need a future semantic-label refresh after the community-set change.

## Mission & iron rules (full text in AGENTS.md)

- No wallpaper parser/renderer/browser/Steam SDK/audio processor may execute in `plasmashell`.
- One issue-sized vertical change per task with acceptance + failure criteria (template: `AI-Skills/TASK_TEMPLATE.md`).
- Separate branches/worktrees per agent/task; implementation and adversarial review are separate passes.
- THIRD_PARTY.yml provenance entry **before** dependent code merges. GPL references = behavior ideas only (ADR 0001), never copied code.
- Synthetic fixtures only; never real Workshop payloads as committed fixtures.
- Bound every queue/allocation/retry/wait/log. Renderer failure must never restart Plasma.
- Compatibility claims need the 6-step parity ladder (docs/FEATURE_COMPATIBILITY.md:109-120); never blanket-claim.

## Commands that work (verified 2026-08-18)

```bash
./scripts/check.sh                       # fmt + clippy -D warnings + cargo test/build + cmake build + qmllint + diagnose + kwe-vulkan
cargo test --workspace                   # Rust unit tests
cd build/cmake && ctest                  # C++ tests (4 targets)
qmllint -I /usr/lib/qt6/qml -I build/cmake/apps/kwe-manager apps/kwe-manager/qml/*.qml
KWE_RUN_WORKSHOP_CACHE_SMOKE=1 ./scripts/check.sh   # smoke suites are opt-in via KWE_RUN_*_SMOKE=1
scripts/smoke-supervisor.sh              # fault-injection (headless-safe)
target/debug/kwe diagnose                # catalog diagnostics
cd packaging && makepkg -f              # release package from local HEAD (commit first!)
KWE_FORCE_AUR_SOURCE=1 makepkg --printsrcinfo   # regenerate .SRCINFO for AUR
```
Install: `sudo pacman -U packaging/kde-wallpaper-engine-0.1.0.alpha.1-2-x86_64.pkg.tar.zst` (sudo needs a password — user runs it).
Post-install handoff: a full KDE/Plasma restart is not required. After installation, run `systemctl --user daemon-reload` and `systemctl --user restart kwe-daemon.service`; if the service is not enabled, use `systemctl --user enable --now kwe-daemon.service`. Reopen `kwe-manager` if it was already running. The package hook does not restart the service automatically. These commands have not been run by the agent; the package is built but not confirmed installed.
Qt is 6.11.1; `kwe-package-installer-test` needs `Qt6::Qml` linked (fixed in 1833331).

## User decisions (2026-08-18, locked)

1. **Scene renderer = full SceneScript engine**, original implementation (QuickJS via rquickjs — MIT, has memory-limit + interrupt controls). Reported per-class/backend-dependent, never blanket.
2. **Web backend = headless Chromium via DevTools Protocol** in the existing bwrap sandbox (frames via `Page.startScreencast` over `--remote-debugging-pipe`).
3. **Live Plasma apply authorized** on this machine, including live-session tests. AGENTS.md's no-live-session rule is explicitly waived by the maintainer for this project's apply work (BETA_M4); destructive tests must still never restart plasmashell.

## Plan status (see BETA_PLAN.md for detail)

| Milestone | Status | Next slice |
|---|---|---|
| BETA_M1 — contract generalization + video renderer | done (M1a–M1e, 2026-08-19) | — |
| BETA_M2 — web renderer (Chromium+CDP) | done (M2a–M2e, 2026-08-19) | — |
| BETA_M3 — scene renderer (QuickJS + ash, slices a–k) | M3a–M3g done; M3h–M3k next | M3h scene3d |
| BETA_M4 — live apply + manager UI (pulled ahead of M3g–M3k) | M4a–M4d done | — |
| BETA_M5 — beta release 0.1.0-beta.1 | pending | last |

Known code gaps the plan resolves (details in BETA_PLAN.md §Found gaps): renderer stderr discarded (G1), env_clear breaks helpers (G2), RLIMIT_AS too small for Chromium (G3), systemd MemoryMax=1G too small (G4), 3s/2s timeouts too tight for real backends (G5), playlists never start renderers (G7), grants not daemon-enforced (G8), missing THIRD_PARTY entries for shipped mpv/bwrap/chromium previews (G9).

## Architecture cheat sheet (for sub-agents)

- **Daemon** (`crates/kwe-daemon`): JSON-RPC over Unix socket (`$XDG_RUNTIME_DIR/kwe/daemon-v1.sock`), one request per connection. Supervisor thread spawns ONE renderer binary per slot: argv `--output <frame.bin> --width --height --fps` (+fault flags), `env_clear()`, stdin=input pipe, stdout=ack pipe, stderr=/dev/null; pre-exec setpgid/PDEATHSIG/no_new_privs/rlimits. Canary = sequence≥3 & ≥1s → AwaitingAck (5s) → promote. Failures → rollback to last-good PPM → quarantine after 3 (key: `wallpaper_id:content_hash`).
- **Frame protocol** (`crates/kwe-frame-protocol`): BGRA8888 premultiplied, 64-byte header `KWEFRM1`, 2-slot seqlock, file = 64+2·w·h·4 ≤512MiB. `SharedFrameWriter::create/publish/set_state`, `SharedFrameReader::snapshot`. C++ consumer `FrameItem` uses pread (no mmap), Frozen after 1.5s.
- **Input protocol** (`crates/kwe-input-protocol`): NDJSON ≤4096B on stdin; `pointer_position` (phases, u16 coords, buttons), renderer acks `input_ack` on stdout; `audio_bands` (16|32|64 f32 bands) and `media_state` wire types exist; since M1d the daemon forwards `audio.forward`/`media.state` RPCs (generation-gated) and `kwe-audio-worker` is the real producer behind `audio_bands` (latest-wins, queue of 1).
- **kwe-core**: `scan.rs` (ProjectKind + per-kind Compatibility — the "planned" strings live at scan.rs:393-411), `preflight_scene` (size/ext checks only), `webpreflight.rs`, `websandbox.rs` (bwrap command builder), `audio.rs` (`analyze_stereo`), `playlist.rs`, `policy.rs` (PermissionPolicy, unconsumed by daemon).
- **kwe-test-renderer**: the contract reference — paced publish loop + InputChannel + fault flags (exit 70/71/72 semantics). New renderers copy it.
- **Manager** (`apps/kwe-manager`): Qt6/QML; Apply hardcoded disabled (WallpaperDetail.qml:98-107); no C++ apply path. VideoPreview spawns mpv, WebPreview spawns bwrap+chromium (both unsandboxed-by-supervisor, both missing THIRD_PARTY entries).
- **Display bridge** (`modules/org/kde/kwe/display` + `plasma/wallpapers/org.kde.kwe.wallpaper`): staged, NOT live-enabled. DisplaySession polls `renderer.status`, sends `renderer.ack`.

## Session log

| Date | Session | What happened | State after |
|---|---|---|---|
| 2026-08-16 | Qwen (prior) | Wrote fix/qt611-gallery-delegates: Qt 6.11 gallery/detail fix + uncommitted hardening changes | Uncommitted diff on branch |
| 2026-08-18 | Claude (this) | Resumed the fix: fixed test target Qt6::Qml link, verified (check.sh, ctest, qmllint, smoke×3), committed `1833331`, built+installed alpha package. Authored beta plan with user decisions. Rebuilt graphify graph. Created AI-Skills setup per maintainer directive. | HEAD 1833331, alpha installed, beta plan pending |
| 2026-08-18 | Claude + sub-agents | BETA_M1a orchestrated: sonnet sub-agent implemented the per-kind renderer contract in worktree `beta-m1a-renderer-contract`; separate Explore reviewer found 12 findings (5 must-fix); same implementer fixed all; verified 104 tests + 18 smoke cases; ff-merged as `cd2d61e`+`62bdbdc`. Key resulting contracts: StartSpec kind/content, per-kind paths/timeouts/limits, per-worker HOME (0700, `runtime/home-<serial>`), bounded stderr ring in status + exit-stderr in failure detail, `audio.forward`/`media.state` with generation-gated forwarding, kind-qualified quarantine identity with legacy migration, systemd 3G/400%/96. Next: BETA_M1b (`kwe-video-renderer`, libmpv). | HEAD 62bdbdc, M1a done |
| 2026-08-18 | Claude + sub-agents | BETA_M1b done (`7a2b402`+`3e83d2e`): `kwe-video-renderer` (libmpv SW render API, bgr0→BGRA8888 premultiplied, paced publish, keepalive, media-state pause/seek, exit 70/71/72/73), smoke-video.sh through the daemon. Review: 3 must-fix + 4 recommended, all fixed; M1a bug surfaced (media/audio acks rejected) fixed with last-wins ack acceptance. | M1b done |
| 2026-08-18/19 | Claude + sub-agents | BETA_M1c done (`51cb469`+`219ebbd`): static `preflight_video` (extension allowlist, ≤2 GiB, non-symlink), scan Video→RendererDependent, `kwe preflight --video`, smoke split (extension reject vs worker-side exit 73); 24 h duration bound implemented for real (fails open on unreadable). BETA_M1d done (`b208465`+`2b2ebd8`): `kwe-audio-worker` + daemon `--audio-capture`/`audio.status` (3 restarts/10 min, then disable), SO_PEERCRED silent-drop. Review: 2 HIGH fixed (nonblocking pw-dump read; ack ceiling max() never decreases). | M1c+M1d done |
| 2026-08-19 | Claude (this session) | BETA_M1e close-out in worktree `kwe-m1e` (branch `beta-m1e-m1-evidence`): per-kind video NPROC knob `--renderer-video-processes` (default 32768, video kind only; smoke workaround removed); `mpv` crate dropped (explicit `extern "C"` + `#[link(name = "mpv")]`, Cargo.lock −201 lines, rust-mpv THIRD_PARTY entry removed); deterministic pixel oracle (solid `#3366CC` mp4 through the daemon, seqlock frame-file parse, 9 pixels within 2 of expected BGRA, tolerance 4; empirical: libmpv aspect-letterboxes); `--probe` → `{"backend":"libmpv","client_api_version":"2.5","libmpv_supports_sw_render":true}`; `kwe diagnose` video lane; FEATURE_COMPATIBILITY content.video implemented; M1 exit gate (92-item corpus clean, 0 global diagnostics); docs updated (SUPERVISOR_API_V1, BETA_M1, FEATURE_COMPATIBILITY, THIRD_PARTY). Gates: 146 tests, smoke-video 11 / supervisor 17 / audio 5. Commit `feat(m1e): ...` at session end. | BETA_M1 done; next BETA_M2a |
| 2026-08-20 | Claude Fable + sub-agents (session 7) | **M4c done** (`e5d69e0` feat by prior session + this session's review cycle): adversarial reviewer (sonnet) found 1 MUST-FIX (user-apply precedence: Busy-as-failure backoff + TOCTOU displacing a fresh user renderer) + 4 recommended + 3 nits; implementer (sonnet) fixed all 8 in `520b2d7` — post-lock `foreign_renderer_live` check with non-failure `ApplyError::Yielded`, apply lane moved to a dedicated bound-1 worker thread (tick thread/API never blocks), stale-store output fallthrough, `Wait` verdict during supervisor recovery, rollback doc honesty; deviation: nothing-live+gate-closed verdict Yield→Hold (anti-storm). Verifier re-review: all CLOSED, 0 new MUST-FIX. Orchestrator independently ran check.sh + smoke-playlist-restart (green, 128 daemon/458 workspace tests), ff-merged, updated plan + this file. NOTE: sessions 2026-08-19 (BETA_M2, M3a–M3f, M4a, M4b) updated BETA_PLAN.md's change log but not this file — see the change log for those details. | M4c merged; M4d next |
| 2026-08-20 | Claude Fable (this session, implementer) | **M4d done** in worktree `kwe-m4d` (branch `beta-m4d-live-enablement`). New `scripts/smoke-live-apply.sh` (opt-in `KWE_RUN_LIVE_APPLY_SMOKE=1`) — DESTRUCTIVE live smoke against the real session: video apply (#3366CC oracle, plasmashell held the frame file open, pixel within tolerance), web apply, hostile scene (`{"general": 42}` → exit 73 → `apply_failed`, rollback preserved the previous assignment), restore (assignment mode, store cleared), end state byte-identical to pre-test; plasmashell PID 919019 unchanged across every run. **Enablement decision:** the smoke daemon binds the REAL runtime socket (system `kwe-daemon.service` stopped first, restarted by the EXIT/INT/TERM trap) so the installed plugin's DisplaySession actually consumes the smoke's frames — proven via `/proc/<plasmashell-pid>/fd`. **Suite-split decision:** smoke-apply.sh's `KWE_LIVE_APPLY=1` lane stays read-only. Failure/recovery proven (injected mid-run failure → exit 1 → full trap restore). Machine state handled honestly: desktop 111 already carried the kwe plugin with an idle system daemon — that is the pre-test baseline. Open risk recorded: user `wallpaper.apply` still blocks the daemon accept loop (transient plugin degraded state during a multi-second apply; self-healing). check.sh + docs/BETA_M4.md + BETA_PLAN.md updated. | M4d in `kwe-m4d`; resume M3g–M3k next |
| 2026-08-20 | Claude Fable (this session, implementer) | **M4d adversarial review fixes** (commit `fix(m4d): adversarial review findings`): 0 MUST-FIX, 4 RECOMMENDED + 4 NIT, all 8 fixed. (1) trap restore escapes the Image via `jq --arg` (the daemon's six-character set) + adds a post-restore verification probe; (2) pre-flight fail-closed for a live non-system kwe-daemon holding the real socket with the service inactive (SIGKILL orphan recovery) — verified live (orphan on the real socket → FAILED with recovery steps, orphan untouched); (3) bounded `is-active` poll after the trap's daemon start with a loud WARNING; (4) `assert_pid_unchanged` requires exactly one plasmashell pid; (5) video fixture 30 s; (6) docs "~4 s" qualified as warm-target; (7) video/web apply failures assert on JSON so the daemon's detail prints; (8) restore replays the captured config-group members (identity-validated). Gates: fmt/clippy/test/check.sh + live smoke green; plasmashell 919019 unchanged; system daemon restored. | M4d committed; resume M3g–M3k next |
| 2026-08-21 | Codex recovery implementer | **BETA_M3g recovered** on `beta-m3g-video-layer-recovery`: shared `kwe-mpv` FFI, scene VideoLayer decode/upload path (≤2), persistent Vulkan refresh, local/package confinement and 160 MiB cap, libmpv local protocol/demux bounds, latest-wins scene media controls, scene-specific NPROC/startup budgets, and fatal fence-timeout propagation across texture/vertex callers. Focused build/clippy green; elevated scene smoke reached all M3g daemon cases and plasmashell guard, but a later standalone subtract oracle was flaky on one run and needs orchestrator rerun. Docs/plan/provenance updated; no UI/live-Plasma changes. | Review + full workspace gates pending |
| 2026-08-22 | Codex recovery implementer + adversarial review | **BETA_M3g merged** as `3b368d2`+`0087c06` after final security-review APPROVE: file videos snapshot through a validated descriptor; destination slots use stable directory-fd `openat`/`unlinkat`; the source cap probes an extra byte without writing; RAII and supervisor cleanup cover staged media; smoke teardown and asynchronous pixel waits are bounded. On exact merged HEAD, `./scripts/check.sh` passed (workspace format/clippy/tests, scene 172, daemon 128, CMake build/tests, diagnostics); `smoke-video.sh` and `smoke-supervisor.sh` passed. The traced full `smoke-scene.sh` passed every M3g daemon/package/corrupt/media/cleanup and standalone llvmpipe lane, with the Plasma PID unchanged. Graphify code map refreshed to 3,826 nodes / 9,682 edges. No UI or live-Plasma changes. | M3g complete on trunk; M3h next |
| 2026-08-22 | Codex + mapper/implementer/reviewer | **Alpha 1 package release 2 built** as `ca3807e`: `pkgrel` bumped 1→2 without changing the application version or M5's planned beta version. Independent review found no packaging findings. Clean `makepkg -Ccf` passed outside the restricted sandbox after the sandbox denied one synthetic Wayland-socket fixture; release workspace tests passed unchanged. New archive metadata is `0.1.0.alpha.1-2`, archive SHA-256 `291ddc58142c2fcbff1ed3f101526871b1a4762c93ba42d25a3877450d4dc624`, and the scene-renderer payload differs from `-1`. No package install or live-session change. Post-install handoff recorded: no full KDE/Plasma restart is needed; reload/restart the user daemon, enable it if necessary, and reopen `kwe-manager`; the commands were not run or installation confirmed. Memory updated for the next handoff. | `-2` archive built; installed package remains `-1`; M3h next |
| 2026-08-22 | Claude Opus 5 (diagnosis + fix) | **Apply lane repaired** (`a747064`) and **Alpha package release 3 built** (`bd60301`). Diagnosed live: the daemon answers `wallpaper.outputs` with `DP-1`, but the manager never asked (listOutputs only reachable from `onVisibleChanged`; the visible pane is created already visible) and could not report anything either (all five clients had `QML_UNCREATABLE` without `QML_ELEMENT` → empty `Module {}` → ReferenceError on every enum binding; 2060 in 6 h of journal). Evidence: instrumented build, per-connection socket relay, `kwe daemon-call`, generated qmltypes. Fixed both, plus outputsListed/resetStatus/request-deadline/failedMethod-notify, `kscreen`+`qt6-tools` deps, `scripts/qml-typecheck.sh` (the old gate called qt5's qmllint), and a QML-error gate in smoke-ui. ctest 8/8, 22 apply-client cases, package verified from the archive against the live daemon. | `-3` archive built and verified; maintainer runs `sudo pacman -U`; M3h next |
| 2026-08-22 | Claude Opus 5 (triage + B1) | **Three user reports triaged into the plan** (new `Open work queue` section in BETA_PLAN.md, order B1→B2→F1) and **B1 fixed** (`3d29272`+`1d332a9` on `beta-b1-display-env`, ff-merged). B1 root cause found live: the unit was `WantedBy=default.target`, reached ~6 s before `graphical-session.target` and before Plasma imports the session environment, so a boot-started daemon had no `WAYLAND_DISPLAY` and `kscreen-doctor` (a QGuiApplication) died on SIGABRT — the picker was empty for a reason unrelated to `a747064`. Fixed on both sides: lazy per-call display recovery from `systemctl --user show-environment` (successes cached, cache dropped when a child fails with it, values validated, budget `min(probe timeout, 1500 ms)`) plus `PartOf`/`After`/`WantedBy=graphical-session.target` with a documented re-enable for existing installs; a real failure now answers `display_unavailable` with the restart the user can run. **Measured and pinned in code:** `WAYLAND_DISPLAY` alone suffices and `QT_QPA_PLATFORM=offscreen` must never be substituted (kscreen-doctor HANGS). New `--systemctl-binary` flag. Gates: 9 unit tests, a stripped-env smoke case **verified to reproduce B1 against the pre-fix binary**, a `display_unavailable` negative control, and a check.sh unit-ordering assertion (verified both ways). check.sh exit 0 (137 daemon tests), ctest 8/8, live read-only smoke green. Also fixed a latent ETXTBSY test flake this work surfaced in the pre-existing external-evaluator test. B2 (scene applies blank: the composite is 518400/518400 pixels of the scene's own 0.7 clearcolor because model layers are skipped as M3h, counted and logged nowhere) is diagnosed and next. | B1 merged, reboot confirmation pending; B2 next, then F1, then M3h |
| 2026-08-22 | Claude Opus 5 (B2) | **B2 fixed** (`77c6d3e` feat + `e6fff36` review on `beta-b2-scene-honesty`, ff-merged). Confirming the reported scene.pkg at fix time found a second defect the diagnosis had not: `parse_objects` treated any object with an `image` KEY as an image layer regardless of value, and the WE editor writes `"image": null` on particle objects — **65 of 65 null-image objects in the local corpus are particle systems**, so every one of them became a textureless image layer and its particle system disappeared (5 of the reported scene's 7 objects). Fixed with one owner of the rule, new `kwe_core::sceneobjects`: `image` classifies only on a string value, and the same classifier answers "can this draw in this build?" for both gates — preflight (file + package lanes, the package lane now decompressing scene.json under the worker's own 16 MiB cap) refuses a scene that declares objects and draws none of them with a named reason per missing feature, and the worker re-checks the same STATIC rule pre-publish (exit 74). Static deliberately: a decode failure still degrades, never rejects (M3c/M3g contract). Policy chosen: **refuse, not degrade to blank** — one drawable object is enough. `model_layer_skips` now counted/reported; the manager shows a feature gap with the current wallpaper left in place. **Scope this exposed:** 46 of 60 local scenes render nothing today (every visual is a model layer behind a TEXV `.tex` texture, or a particle system whose definition is an external file — 380 of 380); they are now refused with a reason. Review found 1 real issue (preflight vs worker disagreed on a textureless-only scene → rollback instead of clean refusal). Gates: check.sh exit 0, ctest 8/8, smoke-scene.sh green with new B2 a/b/d cases; fixed a pre-existing standalone-M3e flake it exposed (sampled the frame before the first publish). | B2 merged; F1 next, then M3h (which now needs a TEXV decoder to matter) |
| 2026-08-22 | Claude Fable 5 (B4 triage) | **B4 filed + diagnosed, no code changes** (`docs/bugs/APPLY_REJECTED_QUARANTINED.md`, plan slices B4a–B4c in BETA_PLAN.md). User: "Applying failed: renderer rejected the start (quarantined)". Three measured causes: (1) unit `TasksMax=96` kills chromium 151.0.7922.173 (upgraded 11:33 today) at bootstrap — `kwe-web-renderer --probe` under `systemd-run -p TasksMax=96` fails (exit 73), 128+ passes, one probe ≥53 tasks; every web apply struck out 3× → quarantined (ids 1747779570, 2646399969); (2) daemon PID 2888 started 14:27 = `-4` binary, `-5` installed 15:56, never restarted → no B2 preflight refusal, `-5` scene worker exit-74 ×3 → quarantine (1652229298); (3) refusals strike like crashes, records have no build identity and never expire, `complete_apply` drops `last_failure_detail`, nothing calls `renderer.retry`; bootstrap stderr tail always empty. Ruled out: RLIMIT_AS/NPROC/NOFILE/env/bwrap/content. Maintainer priority: B4 ahead of F1 ("get all wallpapers to apply first"). Workaround in the bug doc (TasksMax drop-in, restart, clear records). | B4a (unit TasksMax 512 + upgrade restart + diagnose-under-limit) → B4b (refusal ≠ strike, build-id on records, detail + retry in apply) → B4c (drain stderr at bootstrap) → F1 → B3 → M3h |
| 2026-08-22 | Claude Fable 5 (B4 fix) | **B4 fixed** on `beta-b4-apply-quarantine` (worktree `kwe-b4`): B4a `4256c1d` (unit `TasksMax` 96→512 measured; daemon `selfcheck` → `service_stale` on replaced binary, no auto-restart because assignments are not re-applied on start = **B5 filed**; `kwe diagnose` web probe under the unit's TasksMax via systemd-run; post_upgrade restart notice; pkgrel 6), B4b `20ac809` (candidate exit 73/74 = `refused`, no strike/restart/record; `build_id` on supervisor-v1.json, foreign/legacy records dropped at load; `WorkerStatus.quarantined`; `apply_quarantined` with the record detail; `wallpaper.apply retry:true` → supervisor Retry; manager messages + Try Again sends retry once), B4c `1800636` (web renderer drains stderr during bootstrap, `diagnostic_tail()` filters dbus/crashpad noise — under TasksMax=96 it names `Zygote could not fork` / `pthread_create EAGAIN`). Tests: 6 supervisor, 1 RPC, 4 selfcheck, 2 manager; `cargo test --workspace` green (daemon 148), clippy clean, ctest 8/8, `./scripts/check.sh` exit 0. Docs: SUPERVISOR_API_V1 (retry param, apply_quarantined/service_stale, refused class, quarantined field, build scoping), BETA_M4 message table, BETA_M2 TasksMax bound, bug doc §Fix. Not merged to trunk yet; live verification needs `pacman -U` of `-6` + unit restart (journal: `renderer.quarantine_reset dropped=3`). | merge B4 → build `-6` → maintainer installs/restarts/applies web + scene → F1 → B3 → B5 → M3h |
| 2026-08-22 | Claude Fable 5 (B4 live + B6) | **B4 verified live** (`-6` installed, unit restarted: `renderer.quarantine_reset dropped=3`, `kwe diagnose` web lane green under TasksMax=512, web applies promote). **B6 found + fixed** (same branch): applied web wallpapers rendered black — `file://` opaque origin blocks WebGL textures (SecurityError) and same-dir XHR (CORS); chromium `--allow-file-access-from-files` + `--enable-unsafe-swiftshader` added to renderer + preview (kwe-core websandbox, tests). Maintainer chose "enable now + narrow /etc later" (B7 filed); measured exposure: ro-bound system files readable by wallpaper JS, home not bound, network still grant-gated. Compromise suite attempt 2 re-targeted to a host canary outside the binds (green A/B); smoke-web case 7 moved to the B4 refusal contract (`last_failure=refused`, failures 0). 1747779570 black by design (audio-reactive). `pkgrel` 7. | merge → build `-7` → maintainer installs/restarts → apply 2646399969 → F1 |
| 2026-08-22 | Claude Fable 5 (F1) | **F1 scaling modes implemented** on `beta-b4-apply-quarantine`: `ScalingMode` end to end — `wallpaper.apply {scaling}` → `StartSpec` → renderer `--scaling` (video libmpv panscan/keepaspect; scene compositor world-extent mapping from declared resolution; web/test accept) → `renderer.status.scaling` → plugin `DisplaySession.scaling` → `FrameSurface.scaling` (`kweFrameDestination`, clip on fill, pointer normalised against the same rect) → persisted `Assignment.scaling`; manager Aspect/Fill/Stretch ComboBox + `applyWallpaper(..., scaling)`. Render-resolution decision: canvas derived from output geometry (aspect kept, long edge ≤ 2560, `apply::frame_size_for`), explicit width/height still honoured; playlist lane follows. Tests: daemon RPC + unit, scene unit, `kwe-frame-mapping-test` (new ctest), manager test; fake python renderers accept `--scaling`. Docs: API, FEATURE_COMPATIBILITY `display.scaling`, backlog doc, plan. Gates/live verification: see next row. | commit F1 → gates → merge branch to trunk → `-8` package → maintainer verifies modes live → B3 |
| 2026-08-22 | Claude Fable 5 (F1 live, F2, F3/P1 filed) | **F1 verified live on `-8`** (Elden Ring video on DP-1: aspect letterboxed, fill cropped, stretch full; canvas 2560x720 from 2926x823; argv/status/assignment carry the mode). Maintainer: "working well". **F2 implemented** (manager Frame rate limit 15/24/30/60 → `fps`, retry replays, playlist keeps stored fps; test). **F3 designed** (`docs/backlog/PAUSE_WHEN_OCCLUDED.md`: KWin detector + `renderer.pause` + `render_pause` protocol + renderer support). **P1 filed** (`docs/bugs/SLOW_WALLPAPER_CHANGE.md`, measure first). pkgrel 9. | gates → commit → `-9` → merge branch after review → B3 → P1 → F3 |

